\begin{tikzpicture}[
        every node/.style={rectangle, font=\footnotesize, inner sep=3pt},
        every state/.style={rectangle, font=\footnotesize, inner sep=1pt},
        every loop/.style={min distance=1mm, looseness=2},
        auto,
        node distance=7mm and 7mm
    ]

    \node (q1) [state, initial, initial text={}] {
        $\begin{aligned}
            \underline{I} & \to \pointeur \text{si bool E} \\
            \underline{I} & \to \pointeur \text{si bool E sinon E}
        \end{aligned}$
    };

    \node (q2) [state, below=of q1] {
        $\begin{aligned}
            \underline{I} & \to \text{si \pointeur bool E} \\
            \underline{I} & \to \text{si \pointeur bool E sinon E}
        \end{aligned}$
    };

    \node (q3) [state, below=of q2] {
        $\begin{aligned}
            \underline{I} & \to \text{si bool \pointeur E} \\
            \underline{I} & \to \text{si bool \pointeur E sinon E} \\
            \text{E}      & \to \pointeur \text{instr}
        \end{aligned}$
    };

    \node (q4) [state, right=of q3, xshift=30mm] {
        $\begin{aligned}
            \underline{I} & \to \text{si bool E \pointeur} \\
            \underline{I} & \to \text{si bool E \pointeur sinon E}
        \end{aligned}$
    };

    \uncover<3-> {
        \node (boxq4) [draw, rectangle, red, thick] at (q4) {
            \phantom{$\begin{aligned}
                \underline{I} & \to \text{si bool E \pointeur} \\
                \underline{I} & \to \text{si bool E \pointeur sinon E}
            \end{aligned}$}
        };
    }

    \node (q5) [state, right=of q2, xshift=30mm] {
        $\begin{aligned}
            \underline{I} & \to \text{si bool E sinon \pointeur E} \\
            \text{E}      & \to \pointeur \text{instr}
        \end{aligned}$
    };

    \node (q6) [state, above=of q5] {
        $\begin{aligned}
            \underline{I} & \to \text{si bool E sinon E \pointeur}
        \end{aligned}$
    };

    \node (q7) [state, right=of q2, xshift=4mm] {
        $\text{E} \to \text{instr} \pointeur$
    };

    \path[->, every node/.style={font=\footnotesize}]
        (q1)  edge              node                           {si}    (q2)
        (q2)  edge              node                           {bool}  (q3)
        (q3)  edge              node                           {E}     (q4)
        (q4)  edge              node                           {sinon} (q5)
        (q5)  edge              node                           {E}     (q6)
        (q3)  edge              node [auto=right]              {instr} (q7)
        (q5)  edge              node [auto=right]              {instr} (q7);

\end{tikzpicture}
