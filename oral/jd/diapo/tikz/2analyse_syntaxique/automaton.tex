\begin{tikzpicture}[
        every node/.style={rectangle, font=\footnotesize, inner sep=3pt},
        every state/.style={rectangle, font=\footnotesize, inner sep=1pt},
        every loop/.style={min distance=1mm, looseness=2},
        auto,
        node distance=7mm and 7mm
    ]

    \node (q0) [state, initial, initial text={}] {
        $\begin{aligned}
            \underline{P} & \to \pointeur \text{V égal E} \\
            V             & \to \pointeur \text{id}
        \end{aligned}$
    };

    \uncover<4-32>{
        \node (boxq0) [draw, rectangle, red, thick] at (q0) {
            \phantom{$\begin{aligned}
                \underline{P} & \to \pointeur \text{V égal E} \\
                V             & \to \pointeur \text{id}
            \end{aligned}$}
        };
    }

    \node (q1) [state, above=of q0] {
        $ V \to \text{id} \pointeur $
    };

    \uncover<5>{
        \node (boxq1) [draw, rectangle, red, thick, inner ysep=10pt] at (q1) {
            \phantom{$ V \to \text{id} \pointeur $}
        };
        \draw (boxq0) edge[->, red, thick, bend left] node {} (boxq1);
    }

    \node (q2) [state, below=of q0] {
        $ \underline{P} \to \text{V \pointeur égal E} $
    };

    \uncover<7-31>{
        \node (boxq2) [draw, rectangle, red, thick, inner ysep=10pt] at (q2) {
            \phantom{ $ \underline{P} \to \text{V \pointeur égal E} $ }
        };
        \draw (boxq0) edge[->, red, thick, bend right] node {} (boxq2);
    }

    \node (q3) [state, below=of q2] {
        $\begin{aligned}
            \underline{P} & \to \text{V égal \pointeur E} \\
            E             & \to \pointeur \text{ent} \\
            E             & \to \pointeur \text{parg E O E pard}
        \end{aligned}$
    };

    \uncover<8-31>{
        \node (boxq3) [draw, rectangle, red, thick] at (q3) {
            \phantom{ $\begin{aligned}
                \underline{P} & \to \text{V égal \pointeur E} \\
                E             & \to \pointeur \text{ent} \\
                E             & \to \pointeur \text{parg E O E pard}
            \end{aligned}$ }
        };
        \draw (boxq2) edge[->, red, thick, bend right] node {} (boxq3);
    }

    \node (q3bis) [state, below=of q3] {
        $ \underline{P} \to \text{V égal E \pointeur} $
    };

    \uncover<31>{
        \node (boxq3bis) [draw, rectangle, red, thick, inner ysep=10pt]
              at (q3bis) {
            \phantom{$ \underline{P} \to \text{V égal E \pointeur} $}
        };
        \draw (boxq3) edge[->, red, thick, bend right] node {} (boxq3bis);
    }

    \node (q4) [state, right=of q3] {
        $ E \to \text{ent} \pointeur $
    };

    \uncover<10, 17, 23>{
        \node (boxq4) [draw, rectangle, red, thick, inner ysep=10pt] at (q4) {
            \phantom{ $ E \to \text{ent} \pointeur $ }
        };
        \only<10>{\draw (boxq5) edge[->, red, thick, bend left] node {} (boxq4);}
        \only<17>{\draw (box2q5) edge[->, red, thick, bend left] node {} (boxq4);}
        \only<23>{\draw (box2q9) edge[->, red, thick, bend left=35] node {} (boxq4);}
    }

    \node (q5) [state, above right=of q3] {
        $\begin{aligned}
            E & \to \text{parg \pointeur E O E pard} \\
            E & \to \text{\pointeur ent} \\
            E & \to \text{\pointeur parg E O E pard}
        \end{aligned}$
    };

    \uncover<9-29>{
        \node (boxq5) [draw, rectangle, red, thick] at (q5) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg \pointeur E O E pard} \\
                E & \to \text{\pointeur ent} \\
                E & \to \text{\pointeur parg E O E pard}
            \end{aligned}$
            }
        };
        \draw (boxq3) edge[->, red, thick, bend right=10] node {} (boxq5);
    }

    \uncover<16-26>{
        \node (box2q5) [draw, rectangle, red, thick, inner sep=6pt] at (q5) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg \pointeur E O E pard} \\
                E & \to \text{\pointeur ent} \\
                E & \to \text{\pointeur parg E O E pard}
            \end{aligned}$
            }
        };
    }

    \node (q6) [state, above=of q5] {
        $\begin{aligned}
            E & \to \text{parg E \pointeur O E pard} \\
            O & \to \pointeur \text{plus} \\
            O & \to \pointeur \text{fois}
        \end{aligned}$
    };

    \uncover<12-29>{
        \node (boxq6) [draw, rectangle, red, thick] at (q6) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg E \pointeur O E pard} \\
                O & \to \pointeur \text{plus} \\
                O & \to \pointeur \text{fois}
            \end{aligned}$
            }
        };
        \draw (boxq5) edge[->, red, thick, bend right] node {} (boxq6);
    }

    \uncover<19-26>{
        \node (box2q6) [draw, rectangle, red, thick, inner sep=6pt] at (q6) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg E \pointeur O E pard} \\
                O & \to \pointeur \text{plus} \\
                O & \to \pointeur \text{fois}
            \end{aligned}$
            }
        };
        \draw (box2q5) edge[->, red, thick, bend left] node {} (box2q6);
    }

    \node (q7) [state, above left=of q6, xshift=15mm] {
        $ O \to \text{plus} \pointeur $
    };
    \uncover<20>{
        \node (boxq7) [draw, rectangle, red, thick, inner ysep=10pt] at (q7) {
            \phantom{$ O \to \text{plus} \pointeur $}
        };
        \draw (box2q6) edge[->, red, thick, bend right] node {} (boxq7);
    }

    \node (q8) [state, above right=of q6, xshift=-15mm] {
        $ O \to \text{fois} \pointeur $
    };
    \uncover<13>{
        \node (boxq8) [draw, rectangle, red, thick, inner ysep=10pt] at (q8) {
            \phantom{$ O \to \text{fois} \pointeur $}
        };
        \draw (node cs:name=boxq6, anchor=40) edge[->, red, thick, bend right] node {} (boxq8);
    }

    \node (q9) [state, right=of q6] {
        $\begin{aligned}
            E & \to \text{parg E O \pointeur E pard} \\
            E & \to \pointeur \text{ent} \\
            E & \to \pointeur \text{parg E O E pard}
        \end{aligned}$
    };

    \uncover<15-29>{
        \node (boxq9) [draw, rectangle, red, thick] at (q9) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg E O \pointeur E pard} \\
                E & \to \pointeur \text{ent} \\
                E & \to \pointeur \text{parg E O E pard}
            \end{aligned}$
            }
        };
        \draw (node cs:name=boxq6, anchor=-10)
            edge[->, red, thick, bend right] node {}
            (node cs:name=boxq9, anchor=-170);
        \uncover<16-26>{
            \draw (boxq9) edge[->, red, thick, bend left] node {} (box2q5);
        }
    }
    \uncover<22-26>{
        \node (box2q9) [draw, rectangle, red, thick, inner sep=6pt] at (q9) {
            \phantom{
            $\begin{aligned}
                E & \to \text{parg E O \pointeur E pard} \\
                E & \to \pointeur \text{ent} \\
                E & \to \pointeur \text{parg E O E pard}
            \end{aligned}$
            }
        };
        \draw (node cs:name=box2q6, anchor=40)
            edge[->, red, thick, bend left] node {}
            (node cs:name=box2q9, anchor=140);
    }

    \node (q10) [state, below right=of q9, xshift=-25mm] {
        $ E \to \text{parg E O E \pointeur pard} $
    };
    \uncover<25-26, 28-29>{
        \node (boxq10) [draw, rectangle, red, thick, inner ysep=10pt] at (q10) {
            \phantom{$ E \to \text{parg E O E \pointeur pard} $}
        };
        \uncover<25-26>{
            \draw (box2q9) edge[->, red, thick, bend right] node {} (boxq10);
        }
        \uncover<28-29>{
            \draw (boxq9) edge[->, red, thick, bend right] node {} (boxq10);
        }
    }

    \node (q11) [state, below=of q10] {
        $ E \to \text{parg E O E pard \pointeur} $
    };
    \uncover<26, 29>{
        \node (boxq11) [draw, rectangle, red, thick, inner ysep=10pt] at (q11) {
            \phantom{$ E \to \text{parg E O E pard \pointeur} $}
        };
        \draw (boxq10) edge[->, red, thick, bend right] node {} (boxq11);
    }


    \path[->, every node/.style={font=\footnotesize}]
        (q0)  edge              node [swap]                    {id}    (q1)
              edge              node                           {V}     (q2)
        (q2)  edge              node                           {égal}  (q3)
        (q3)  edge              node [auto=right]              {ent}   (q4)
              edge              node                           {parg}  (q5)
              edge              node                           {E}     (q3bis)
        (q5)  edge [loop right] node [yshift=4mm, xshift=-1mm] {parg}  ()
              edge              node                           {ent}   (q4)
              edge              node                           {E}     (q6)
        (q6)  edge              node [yshift=2.5mm]            {plus}  (q7)
              edge              node [yshift=-2.5mm]           {fois}  (q8)
              edge              node                           {O}     (q9)
        (q9)  edge              node [auto=right, yshift=-1mm] {parg}  (q5)
              edge [bend left]  node                           {ent}   (q4)
              edge              node [yshift=-2.1mm]           {E}     (q10)
        (q10) edge              node                           {pard}  (q11);

\end{tikzpicture}
