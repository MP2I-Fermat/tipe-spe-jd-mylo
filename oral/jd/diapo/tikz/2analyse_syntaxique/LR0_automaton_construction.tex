\begin{tikzpicture}[
        every node/.style={rectangle, font=\footnotesize, inner sep=3pt},
        every state/.style={rectangle, font=\footnotesize, inner sep=1pt},
        every loop/.style={min distance=1mm, looseness=2},
        auto,
        node distance=7mm and 7mm
    ]

    \node (q0) [state, initial, initial text={}] {
        $\begin{aligned}
            \axiomcolour{P}  & \to \pointeur \ntcolour{V}~\text{égal}~\ntcolour{E} \\
            \uncover<2->{\ntcolour{V} & \to \pointeur \text{id}}
        \end{aligned}$
    };

    \uncover<4->{
        \node (q1) [state, above right=of q0] {
            $ \ntcolour{V} \to \text{id} \pointeur $
        };
    }

    \uncover<5->{
        \node (q2) [state, below right=of q0] {
            $ \axiomcolour{P} \to \ntcolour{V} \pointeur \text{égal}~\ntcolour{E} $
        };
    }

    \uncover<7->{
        \node (q3) [state, right=of q2] {
            $\begin{aligned}
                \axiomcolour{P} & \to \ntcolour{V}~\text{égal} \pointeur \ntcolour{E} \\
                \uncover<8->{%
                \ntcolour{E}    & \to \pointeur \text{ent} \\
                \ntcolour{E}    & \to \pointeur \text{parg}~\ntcolour{E O E}~\text{pard}
                }%
            \end{aligned}$
        };
    }

    \uncover<3->{
        \path[->, every node/.style={font=\footnotesize}]
            (q0) edge node {id}           (q1);
        \path[->, every node/.style={font=\footnotesize}]
            (q0) edge node {\ntcolour{V}} (q2);
    }
    \uncover<6->{
        \path[->, every node/.style={font=\footnotesize}]
            (q2) edge node {égal}         (q3);
    }

\end{tikzpicture}
