\begin{center}
\begin{tikzpicture}[node distance=0mm and 30mm]
    \matrix (chars) [
        matrix of nodes,
        row sep=-\pgflinewidth,
        column sep=-\pgflinewidth,
        nodes={draw, rectangle, align=center, text width=1.25ex},
        nodes in empty cells,
        text depth=0.25ex,
        text height=1ex
    ] {
        \texttt{l} &
        \texttt{e} &
        \texttt{t} &
        \texttt{ } &
        \texttt{p} &
        \texttt{l} &
        \texttt{u} &
        \texttt{s} &
        \texttt{\_} &
        \texttt{u} &
        \texttt{n} &
        \texttt{ } &
        \texttt{x} &
        \texttt{ } &
        \texttt{=} &
        \texttt{ } &
        \texttt{x} &
        \texttt{+} &
        \texttt{1} \\
    };

    \matrix (tokens) [
        matrix of nodes,
        row sep=-\pgflinewidth,
        column sep=-\pgflinewidth,
        nodes={draw=vertFonce, thick, rectangle, align=center, visible on=<4->},
        nodes in empty cells,
        text depth=0.25ex,
        text height=1ex,
        below=of chars
    ] {
        \mintinline{ocaml}{let} &
        \texttt{plus\_un} &
        \texttt{x} &
        \mintinline{ocaml}{=} &
        \texttt{x} &
        \mintinline{ocaml}{+} &
        \mintinline{ocaml}{1} \\
    };

    \node[below=of tokens-1-1, visible on=<5->, yshift=-10mm] (types-1) {let};
    \node[below=of tokens-1-2, visible on=<6->, yshift=-5mm] (types-2) {identifiant};
    \node[below=of tokens-1-3, visible on=<7->, yshift=-10mm] (types-3) {identifiant};
    \node[below=of tokens-1-4, visible on=<8->, yshift=-5mm] (types-4) {égal};
    \node[below=of tokens-1-5, visible on=<9->, yshift=-15mm] (types-5) {identifiant};
    \node[below=of tokens-1-6, visible on=<10->, yshift=-10mm] (types-6) {plus};
    \node[below=of tokens-1-7, visible on=<11->, yshift=-5mm] (types-7) {entier};

    \draw[thick, ->, visible on=<5->] (tokens-1-1) -- (types-1);
    \draw[thick, ->, visible on=<6->] (tokens-1-2) -- (types-2);
    \draw[thick, ->, visible on=<7->] (tokens-1-3) -- (types-3);
    \draw[thick, ->, visible on=<8->] (tokens-1-4) -- (types-4);
    \draw[thick, ->, visible on=<9->] (tokens-1-5) -- (types-5);
    \draw[thick, ->, visible on=<10->] (tokens-1-6) -- (types-6);
    \draw[thick, ->, visible on=<11->] (tokens-1-7) -- (types-7);

\end{tikzpicture}
\end{center}
